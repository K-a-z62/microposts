<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong id="follow" class="stats">
      <%= @user.following_users.count %>
    </strong>
    フォロー
  </a>
 
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stats">
      <%= @user.follower_users.count %>
    </strong>
    フォロワー
  </a>
  
</div>